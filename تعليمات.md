# نظام تسجيل الحضور للطلبة | Student Attendance System

نظام إدارة متكامل لتسجيل حضور الطلبة بالمختبرات مع واجهة عربية حديثة.

## المميزات
- 📝 تسجيل الطلبة (الاسم، الرقم الامتحاني، المرحلة، الشعبة، المختبر)
- 🔍 بحث متقدم عن الطلبة وسجل حضورهم
- ✅ تسجيل الحضور بالمادة والتاريخ
- 📊 تصدير تقارير Excel و PDF بدعم كامل للعربية
- 🗂️ إدارة الطلبة (تعديل/حذف/استيراد من Excel)
- 🌓 واجهة حديثة مع وضع داكن

## المتطلبات
- Python 3.10+
- تثبيت الحزم:

```bash
pip install -r requirements.txt
```

## التشغيل (بيئة تطوير)

```bash
python run.py
```

سيفتح الموقع على `http://127.0.0.1:5000`.

## الصفحات
- **الرئيسية**: إحصائيات سريعة (عدد الطلبة، حضور اليوم، غياب اليوم)
- **تسجيل الطلبة**: إضافة طالب جديد يدوياً
- **البحث**: البحث بالاسم أو الرقم الامتحاني مع عرض سجل الحضور الكامل
- **تسجيل الحضور**: تسجيل حضور/غياب مع اختيار المرحلة/الشعبة/المختبر/المادة
- **إدارة الطلبة**: تعديل/حذف الطلبة + استيراد من Excel
- **تصدير التقارير**: تصدير Excel و PDF مع فلاتر شاملة (المادة/المرحلة/الشعبة/المختبر/التاريخ)

يتم حفظ قاعدة البيانات في ملف `attendance.db` داخل نفس المجلد (SQLite).